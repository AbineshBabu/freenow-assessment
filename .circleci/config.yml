version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run
      - npm run posttest
          post-steps:
            - store_artifacts:
                path: cypress/report
